@model WebNhaHang.Models.ApplicationUser

@{
    ViewData["Title"] = "Đăng Ký - Register";
}

<div class="container mx-auto px-4 py-16">
    <div class="max-w-md mx-auto">
        <div class="bg-white rounded-lg shadow-lg p-8">
            <div class="text-center mb-8">
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Đăng Ký Tài Khoản</h1>
                <p class="text-gray-600">Tạo tài khoản để trải nghiệm dịch vụ tốt nhất</p>
            </div>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
                    @TempData["SuccessMessage"]
                </div>
            }

            <form asp-action="Register" method="post" class="space-y-4">
                @Html.AntiForgeryToken()

                <!-- Full Name -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-2">
                        Họ và Tên <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="FullName" type="text" required
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500"
                           placeholder="Nguyễn Văn A">
                    <span asp-validation-for="FullName" class="text-red-500 text-sm"></span>
                </div>

                <!-- Email -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-2">
                        Email <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="Email" type="email" required
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500"
                           placeholder="email@example.com">
                    <span asp-validation-for="Email" class="text-red-500 text-sm"></span>
                </div>

                <!-- Username -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-2">
                        Tên Đăng Nhập <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="Username" type="text" required
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500"
                           placeholder="username">
                    <span asp-validation-for="Username" class="text-red-500 text-sm"></span>
                </div>

                <!-- Phone -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-2">
                        Số Điện Thoại
                    </label>
                    <input asp-for="Phone" type="tel"
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500"
                           placeholder="0123456789">
                    <span asp-validation-for="Phone" class="text-red-500 text-sm"></span>
                </div>

                <!-- Password -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-2">
                        Mật Khẩu <span class="text-red-500">*</span>
                    </label>
                    <input name="password" type="password" required minlength="6"
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500"
                           placeholder="Tối thiểu 6 ký tự">
                </div>

                <!-- Confirm Password -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-2">
                        Xác Nhận Mật Khẩu <span class="text-red-500">*</span>
                    </label>
                    <input name="confirmPassword" type="password" required minlength="6"
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500"
                           placeholder="Nhập lại mật khẩu">
                </div>

                <!-- Terms -->
                <div class="flex items-start">
                    <input type="checkbox" required class="mt-1 mr-2">
                    <label class="text-sm text-gray-600">
                        Tôi đồng ý với <a href="#" class="text-orange-500 hover:underline">Điều khoản sử dụng</a>
                        và <a href="#" class="text-orange-500 hover:underline">Chính sách bảo mật</a>
                    </label>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="w-full bg-orange-500 text-white px-6 py-3 rounded-lg hover:bg-orange-600 transition font-semibold">
                    Đăng Ký
                </button>

                <!-- Login Link -->
                <div class="text-center text-gray-600">
                    Đã có tài khoản?
                    <a asp-action="Login" class="text-orange-500 hover:underline font-semibold">Đăng nhập ngay</a>
                </div>
            </form>
        </div>

        <!-- Benefits -->
        <div class="mt-6 bg-orange-50 border border-orange-200 rounded-lg p-6">
            <h3 class="font-bold text-gray-800 mb-3">🎁 Quyền lợi thành viên</h3>
            <ul class="space-y-2 text-sm text-gray-700">
                <li class="flex items-start">
                    <span class="text-orange-500 mr-2">✓</span>
                    <span>Tích điểm và nhận ưu đãi đặc biệt</span>
                </li>
                <li class="flex items-start">
                    <span class="text-orange-500 mr-2">✓</span>
                    <span>Theo dõi lịch sử đơn hàng và đặt bàn</span>
                </li>
                <li class="flex items-start">
                    <span class="text-orange-500 mr-2">✓</span>
                    <span>Nhận thông báo về sự kiện và khuyến mãi</span>
                </li>
                <li class="flex items-start">
                    <span class="text-orange-500 mr-2">✓</span>
                    <span>Đặt hàng nhanh chóng hơn</span>
                </li>
            </ul>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Password match validation
        document.querySelector('form').addEventListener('submit', function(e) {
            const password = document.querySelector('input[name="password"]').value;
            const confirmPassword = document.querySelector('input[name="confirmPassword"]').value;

            if (password !== confirmPassword) {
                e.preventDefault();
                alert('Mật khẩu xác nhận không khớp!');
            }
        });
    </script>
}
